export const metadata = {
    title: "Gerenciar usuários - Biblioteca FEAL",
    robots: { index: false, follow: false }
};

import UsuariosPage from "./usuariosPage";

export default function Page() {
    return <UsuariosPage />;
}
