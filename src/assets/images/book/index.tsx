import type { StaticImageData } from "next/image";

import default_cover from "assets/images/book/cover/default-cover.png";

export const bookCover: Record<string, StaticImageData> = {
    default: default_cover
};
