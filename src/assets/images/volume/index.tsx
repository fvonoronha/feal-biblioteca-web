import type { StaticImageData } from "next/image";

import default_volume_cover from "assets/images/volume/cover/default-cover.png";

export const volumeCover: Record<string, StaticImageData> = {
    default: default_volume_cover
};
