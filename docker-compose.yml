version: "3.8"

services:
    app:
        ports:
            - "3000:3000"
        build:
            context: .
        volumes:
            - /app/node_modules
            - /app/.next
        restart: always
